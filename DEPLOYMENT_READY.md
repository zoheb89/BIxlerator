# Deployment ready

The production-hardening branch is prepared for Render deployment. Review and merge this branch into `main`, then connect the repository to Render using the existing `render.yaml` Blueprint.
